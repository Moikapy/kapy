// DEFERRED: OAuth CLI is deferred per ADR-015. This file is a stub.
// When cloud providers are re-enabled, restore the full CLI.

console.log("kapy-ai CLI is deferred. See ADR-015.");
process.exit(0);
